package exercises
import scala.math._

class Exercise1 {
  def getSquare(num: Int) = { //don't forget = sign
    sqrt(num)
  }
  def isArg1GreaterThanArg2(num1: Double, num2: Double) = {
    num1 > num2
   // if (num1 > num2) true
   // else false
  }
  def manyTimesString(str: String, num: Int) = {
    str * num
  }

}